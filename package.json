{
  "name": "client-lead-management-root",
  "version": "1.0.0",
  "description": "Root orchestration for client lead management project",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
